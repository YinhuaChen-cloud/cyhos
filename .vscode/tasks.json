{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build native-shell",
            "type": "shell",
            "command": "make build/native-shell.elf"
        },

        {
            "label": "Build native",
            "type": "shell",
            "command": "make build/native.elf"
        }
    ]
}